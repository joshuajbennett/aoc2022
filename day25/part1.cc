#include <atomic>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <mutex>
#include <optional>
#include <queue>
#include <sstream>
#include <string>
#include <thread>
#include <unordered_map>
#include <unordered_set>
#include <vector>

int snafuToDecimal(const std::string& input) {
  for (int i = 0; i < input.size(); ++i) {
    char current = input[input.size() - 1 - i];
  }
}

std::string decimalToSnafu(const int number) {}

int main(int argc, char** argv) {
  std::string line;
  std::vector<std::string> lines;
  while (std::getline(std::cin, line)) {
    lines.push_back(line);
  }
  return 0;
}
